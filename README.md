# Desktop Countdown · 桌面倒计时

一款简单、美观、稳定的桌面倒计时小工具。

- 版本：v0.11.0
- 支持系统：Windows 10 / 11 64 位；macOS（Apple Silicon / Intel）

## 项目简介

以倒计时数字为视觉中心的极简效率工具。v0.2.0 全面升级为**多任务多窗口**架构：支持同时运行多个时长倒计时与日期/纪念日倒计时，每个任务可单独打开悬浮窗、极简窗或全屏模式；新增年度/月度/今日进度、任务完成记录与查询删除、记录自动推送 Git。所有数据仅保存在本地。

## 技术栈

- Electron 33（主进程统一管理任务状态，多窗口广播同步）
- Vue 3.5（`<script setup lang="ts">` Composition API）
- TypeScript（严格模式，主进程/渲染层共享 `shared/types.ts`）
- Vite 5 + electron-vite 2
- Pinia 3（tasksStore / settingsStore，渲染层为 IPC 镜像）
- electron-builder 25（Windows NSIS 安装包）

## 安装依赖

```bash
npm install        # Node.js >= 20，已配置 npmmirror 镜像
```

## 开发运行

```bash
npm run dev        # electron-vite 开发环境（热更新）
```

## 单元测试与类型检查

```bash
npm test           # 时间格式化 / 年月日进度 / 日期倒计时等 9 项测试
npm run typecheck  # vue-tsc 全量类型检查（main + renderer）
```

## 构建与打包

```bash
npm run build      # 构建主进程 / Preload / 渲染进程到 out/
npm run dist       # 构建并打包 Windows NSIS 安装程序
```

产物位于 `release/` 目录：`桌面倒计时-Setup-0.11.0.exe`。安装后无需 Node.js 环境，可独立运行。

### macOS 打包

在 **macOS 电脑**上克隆本仓库后执行：

```bash
npm install
npm run dist:mac
```

产物：`release/桌面倒计时-macOS-0.7.1-arm64.dmg` 与 `-x64.dmg`（同时输出 zip）。DMG 拖入"应用程序"即可使用。

> 说明：electron-builder 不支持在 Windows 上构建 macOS 包，因此 DMG 必须在 Mac 上生成；本项目未做开发者签名与公证，首次打开若被 Gatekeeper 拦截，请**右键应用 → 打开**，或执行 `xattr -dr com.apple.quarantine /Applications/桌面倒计时.app`。macOS 版已适配：红绿灯窗口控制（隐藏式内嵌标题栏）、⌘+Alt+T 全局快捷键、开机启动等。

## 目录结构

```
├─ electron/
│  ├─ main/            # 主进程：多任务 TaskManager、多窗口、托盘、IPC、记录、自动推送、日志
│  └─ preload/         # contextBridge 安全暴露白名单 API
├─ shared/             # 主进程与渲染层共享类型
├─ src/
│  ├─ assets/          # 全局样式（浅色/深色主题、渐变、圆角卡片）
│  ├─ components/      # TaskCard / NewTaskForm / ProgressStats / HistoryList / TaskFloat / TaskScreen 等
│  ├─ composables/     # useShortcuts（窗口内快捷键）
│  ├─ services/        # sound（提示音播放）
│  ├─ stores/          # Pinia：tasks / settings（IPC 镜像 store）
│  ├─ types/           # 渲染层类型（re-export shared）
│  ├─ utils/           # 时间格式化 / 年月日进度纯函数
│  ├─ views/           # Dashboard（主仪表盘）/ TaskWindow（任务小窗）
│  ├─ App.vue          # 按 URL 参数路由（?view=task&id=..&mode=..）
│  └─ main.ts
├─ public/             # finish.wav（渲染进程提示音）
├─ resources/          # 图标（icon.ico / icon.png）与提示音
├─ scripts/            # 资源生成脚本（纯 Node 生成图标与 WAV）
├─ tests/              # 单元测试
├─ electron-builder.yml
└─ electron.vite.config.ts
```

## 功能列表

### v0.11.0 变更

- **最后 60 秒秒数大字显示**：所有任务剩余不足 60 秒时，主显示切换为"X 秒"大字（替代分钟），覆盖全部窗口
- **修复**：任务完成 30 秒自动消失后，其悬浮窗/极简窗/全屏窗会**自动关闭**，不再残留"任务已删除"的无效窗口（并保留手动关闭兜底按钮）

### v0.10.0 变更

- **整合面板任务可点击跳转主界面**；提醒触发时主页以最高优先级（临时强制置顶）弹到桌面最前端
- **已完成任务 30 秒后自动消失**：不再出现在主界面与整合面板（期间可"重新武装"打断消失）
- **移除记录板块**：记录页、完成记录存储、记录自动推送 Git、设置里的"清空历史任务"按钮全部删除；主页不再累计显示"已完成 N 个"
- **标题栏新增 📌 置顶/取消置顶按钮**（最小化旁边，置顶中高亮）

### v0.9.0 新增

- **统一时间分层显示**：所有任务剩余**不足 60 分钟**时，主显示单位直接替换为"X 分钟"（原本的小时位/天数位变为分钟，下方小字显示剩余秒数）；60 分钟~24 小时显示"X 小时"+分秒；24 小时以上显示"X 天"+时分秒
- 覆盖全部位置：任务卡片、悬浮窗、极简窗、全屏、📋 总览、🧩 整合面板；时长/定点/纪念日三类任务一致

### v0.8.1 新增

- **设置 → 关于：版本号自动显示**——改为运行时读取 `app.getVersion()`（即 package.json 的版本），每次发版"关于"自动同步，不再手工维护硬编码字符串；配套验证脚本 `scripts/verify-about.mjs` 可随时复检

### v0.8.0 新增

- **设置 → 数据：清空历史已完成任务**（二次确认防误触）——一键清掉已完成的任务与完成记录，主页"已完成 N 个"计数从零重新开始；运行中/待开始任务与设置不受影响
- **单实例限制**：使用 `app.requestSingleInstanceLock()`，重复点击桌面快捷方式/开始菜单只会把已运行的唯一实例窗口弹到前台，不再开出多个应用

### v0.7.1 新增

- **macOS 安装包支持**：electron-builder 增加 mac 配置（DMG + ZIP，Apple Silicon 与 Intel 双架构，图标自动由 icon.png 转换），`npm run dist:mac` 一键构建
- 跨平台适配：macOS 使用 hiddenInset 标题栏（保留红绿灯按钮，自绘按钮自动隐藏）、全局快捷键改为 ⌘/Ctrl+Alt+T、暴露 platform 信息给渲染层
- 注：DMG 需在 macOS 上构建（electron-builder 限制），Windows 本机已验证配置与代码通过全部测试

### v0.7.0 新增

- **任务拖拽排序**：主页任务列表与 🧩 整合面板的任务都可直接上下拖拽调整位置（目标行顶部显示蓝色插入线），顺序持久化并在所有窗口同步
- **任务重命名**：任务卡片新增 ✏ 按钮（或双击标题）即可改名，**运行中/暂停中的任务也可修改**，回车或失焦保存、Esc 取消，新名称实时同步全部窗口

### v0.6.1 新增

- **定点倒计时智能显示**：剩余不足 24 小时不再显示"0 天"，改为主显示剩余小时数（如"5 小时"+ 分秒），≥24h 仍显示天数 + 时分秒；卡片/悬浮/极简/全屏/总览/整合面板全部生效

### v0.6.0 新增

- **⏰ 定点倒计时**：指定目标日期 + 时刻（如 2026-08-20 09:00），创建即自动运行，倒计时精确到该时刻；到点触发完整提醒链（20 次铃声 + 系统通知 + 自动弹出主页 + 完成记录）
- 新建表单排布：定点倒计时 / 时长倒计时 / 日期·纪念日，定点模式排在时长之前并默认选中
- 定点任务卡片显示"X 天 + HH:MM:SS"实时倒计时与目标时刻，支持"重新武装"；悬浮/极简/全屏/总览/整合面板全部适配（⏰ 图标）

### v0.5.1 新增

- **修复**：三合一进度悬浮窗与整合面板中"今日"进度条因 emoji 宽度差异未与年/月条对齐，现三条进度条严格左对齐
- **任务到点自动弹出主页**：提醒触发时主进程自动把主窗口弹到前台，配合铃声与横幅确保不错过

### v0.5.0 新增

- **整合面板改为正方形并再缩小一半**（204×204，最小 176×176）：进度三行 + 全部任务一屏整合，面积仅为主窗口约 1/6
- **任务区自动填充**：任务少时每行自动拉伸填满面板不留空白，多时紧凑滚动；无任务显示"🎉 全部完成"
- **全部悬浮窗美化**：统一渐变主色顶条与渐变底色、运行中任务左侧高亮竖条 + 呼吸角标、单项进度图标软底托，更精致统一

### v0.4.1 新增

- **🧩 整合面板**：一个小长方形悬浮窗（252×388，可拖拽调整大小）把全部提醒任务与年度/月度/今日进度合并显示——顶部三行进度条，下方任务列表（彩色名称 + 大号时间/天数）实时同步；主页右上角 🧩 按钮或托盘菜单"打开整合面板"均可唤出

### v0.4.0 新增

- **铃声连续提醒**：结束后 30 秒内连续提醒 20 次（每 1.5 秒一次双音铃声），仍可一键手动停止
- **进度单项悬浮**：年度/月度/今日进度既可整合悬浮（三合一窗），也可每项单独悬浮（大号百分比）
- **任务总览悬浮窗**：📋 一窗整合显示所有提醒任务（彩色名称 + 时间/天数 + 纪念日时分秒），与主界面实时同步
- **任务名称彩色大字**：按任务 id 稳定取色（8 色循环），字号加大，所有窗口（主页/悬浮/极简/全屏/总览）显示一致
- **纪念日倒计时升级**：日期任务显示"X 天 + HH:MM:SS"实时倒计时
- 各悬浮窗新增语义小图标（📅 年度 / 🌙 月度 / ☀️ 今日 / ⏱ 时长 / 🗓 纪念日 / 📋 总览），时间与天数均突出大字显示
- 主页欢迎语改为"今天也要开心呀，今天也要加油啊！"

### v0.3.0

- **进度悬浮窗**：年度/月度/今日进度可独立悬浮显示（232×172 三项 / 180×64 仅今日），常驻桌面置顶
- **托盘单击即显**：单击托盘图标直接显示主窗口，右键仍为菜单
- **提示音延长至 30 秒**（每 5 秒一轮铃声，结尾和弦收音）；播放中主界面弹出**可手动关闭铃声**的提醒横幅
- **UI 配色全面升级**（ui-ux-pro-max 设计数据驱动）：Indigo 主色系 + 渐变、三色语义进度条（年度靛蓝 / 月度紫罗兰 / 今日翡翠绿）、键盘焦点环（WCAG AA）

### v0.2.0

- **多任务倒计时**：任意数量任务同时计时，互不干扰；主进程统一计时（`endTime - Date.now()`），多窗口实时同步
- **日期 / 纪念日倒计时**：选择目标日期，按天显示剩余天数与推进百分比
- **进度条与百分比**：每个任务卡片实时进度；**年度 / 月度 / 今日进度**独立页面（含已过/剩余天数）
- **多窗口显示**：每个任务可单独打开**悬浮窗**（230×118，可拖拽置顶）、**极简窗**（168×74，仅时间，移入显示操作）、**全屏模式**（大字号演示模式，Esc 退出）
- **完成记录**：倒计时结束自动记录，可按关键词/类型查询，可删除
- **记录自动推送 Git**：设置中开启并指定仓库路径后，完成记录防抖 30 秒提交推送到仓库 `records/records.json`
- **UI 全面美化**：渐变主色、圆角卡片、状态标签、任务角标、悬浮动效；悬浮窗比 v0.1 缩小约 1/3

### 基础功能（v0.1.0 起）

- 快捷预设 5/10/15/25/30/60 分钟 + 自定义时分秒；开始/暂停/继续/重置
- 始终置顶、透明度 60%~100%、窗口位置记忆（越屏自动回正）
- 系统托盘常驻、Windows 通知、提示音（音量可调）
- 开机自动启动、浅色/深色/跟随系统主题、本地配置持久化

## 快捷键

| 按键 | 作用 |
| ---- | ---- |
| Space | 开始 / 暂停当前任务（输入框内不触发） |
| R | 重置运行中的任务 |
| Esc | 关闭设置 / 退出全屏 |
| Ctrl+Alt+T（全局） | 显示 / 隐藏主窗口 |

## 已知问题

- 本机杀毒软件可能间歇性锁定新编译的 exe，导致 electron-builder 偶发失败，重跑即可
- 图标为脚本生成的临时占位图标
- 悬浮/极简窗口尺寸固定；Windows“专注助手”开启时通知可能不显示
- 自动推送要求配置的目录是 Git 仓库且有推送权限；安装版默认关闭

## 路线图（下一版本）

- 任务重命名与编辑时长
- 倒计时结束前阶段性提醒（剩余 1 分钟）
- 悬浮窗尺寸/字体自定义
- 完成记录导出 CSV
- 番茄工作法统计与热力图
