export type {
  Task,
  TaskType,
  TaskStatus,
  FinishRecord,
  WindowBounds,
  ThemeMode,
  AppConfig
} from '../../shared/types'

import type { AppConfig } from '../../shared/types'
export type Settings = AppConfig
