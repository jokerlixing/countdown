export type {
  Task,
  TaskType,
  TaskStatus,
  WindowBounds,
  ThemeMode,
  AppConfig
} from '../../shared/types'

import type { AppConfig } from '../../shared/types'
export type Settings = AppConfig
